import 'package:flutter/material.dart';

class Gordon extends StatelessWidget {
  const Gordon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Hello'),
    );
  }
}
